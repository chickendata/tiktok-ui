import { faMagnifyingGlass } from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

function SearchIcon() {
    return (
        <div>
            <FontAwesomeIcon icon={faMagnifyingGlass} />;<h2>abc</h2>
        </div>
    );
}

export default SearchIcon;
